# Dependencies
node_modules/

# Backend secrets
backend/.env
*.log

# Database backups — contain every record plus password hashes, must never
# be pushed to a remote. Keep them on disk / in synced cloud storage only.
backend/backups/
backups/

# Expo / mobile
mobile/.expo/
mobile/dist/
mobile/web-build/
mobile/expo-env.d.ts
mobile/.kotlin/
mobile/*.orig.*
mobile/*.jks
mobile/*.p8
mobile/*.p12
mobile/*.key
mobile/*.mobileprovision
mobile/.metro-health-check*
mobile/npm-debug.*
mobile/yarn-debug.*
mobile/yarn-error.*
mobile/*.tsbuildinfo
mobile/ios/
mobile/android/

# OS
.DS_Store
