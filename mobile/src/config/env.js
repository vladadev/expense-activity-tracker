// During local development with Expo Go, "localhost" refers to the PHONE, not
// your PC — so it must point at your computer's LAN IP address instead.
// Find it with `ipconfig` (look for IPv4 Address) and update API_BASE_URL below.
// Once the backend is deployed to Render (final phase), replace this with the
// deployed URL, e.g. https://your-app.onrender.com/api
export const API_BASE_URL = 'http://192.168.0.24:4000/api';
