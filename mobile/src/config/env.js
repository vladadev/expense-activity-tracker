// Deployed backend on Render — works from anywhere with internet, not tied
// to being on the same WiFi network as a dev machine anymore.
export const API_BASE_URL = 'https://expense-activity-tracker.onrender.com/api';
