// Expense/event categories are user-editable now (see CategoriesContext) —
// only these two axes stay fixed since they're structural, not user content.
export const EXPENSE_TYPES = ['personal', 'together'];
export const CURRENCIES = ['RSD', 'EUR', 'USD'];
