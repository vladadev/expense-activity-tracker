# Expo version

This app is on **Expo SDK 54** (React Native 0.81.5). Read the exact versioned
docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code — API
surfaces and platform support differ between SDK versions, and the docs for a
version we are not on will describe behaviour this app does not have.

Check `package.json` before trusting this line; if the SDK has been upgraded,
update the link here in the same change.
